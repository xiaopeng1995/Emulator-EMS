apply plugin: 'application'

mainClassName = "io.j1st.test.TestEmulator"

dependencies {
    // project
    compile project(':emulator-util')
    // project
    compile project(':emulator-mongo')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}